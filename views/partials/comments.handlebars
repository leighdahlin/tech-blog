<div class="card border-secondary mb-3 post">
    <div class="card-header display-comment">
        <h6>{{user.name}}</h6>
        <h6 class="date">{{format_time created_at}}</h6>
    </div>
    <div class="card-body text-secondary">
        <p class="card-text">{{content}}</p>
    </div>
</div>
